# Choose airbuild image version
DOCKER_IMAGE="jeko/airbuild:15.0"

# Remove SUDO if not necessary
DOCKER_RUN="sudo docker run -v `pwd`:/src -it --rm $DOCKER_IMAGE"

# No need to edit that
MXMLC="$DOCKER_RUN mxmlc"
ADT="$DOCKER_RUN adt"
